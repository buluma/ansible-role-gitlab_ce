---
- hosts: all
  remote_user: root
  become: true
  tasks:
    - name: include role
      ansible.builtin.include_role:
        name: buluma.bootstrap

    - name: include role
      ansible.builtin.include_role:
        name: gitlab_ce
      tags: gitlab_ce
